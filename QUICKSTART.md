# 快速开始指南 | Quick Start Guide

## � 快速唤醒项目（2 分钟启动）

如果您是第一次使用或重新打开项目，按照以下步骤操作：

### 方法 1：一键启动（推荐）✨

```bash
# 1. 进入项目目录
cd d:\code\china-footprint-map

# 2. 启动开发服务器（如果已安装依赖）
npm run dev
```

浏览器会自动打开 `http://localhost:3000`，您就可以看到地图了！

---

### 方法 2：完整启动流程

如果是全新项目或遇到问题，按以下完整步骤操作：

```bash
# 步骤 1: 进入项目目录
cd d:\code\china-footprint-map

# 步骤 2: 安装依赖（首次运行或依赖更新后需要）
npm install

# 步骤 3: 下载地图数据（已自动下载，可跳过）
# node scripts/download-cities-detailed.js

# 步骤 4: 启动开发服务器
npm run dev
```

---

### ✅ 项目启动成功标志

当您看到以下输出时，说明项目已成功启动：

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

然后：

1. 浏览器会自动打开 `http://localhost:3000`
2. 您会看到完整的中国地图
3. 点击任意城市开始记录足迹！

---

### 🔧 常见启动问题

#### 问题：npm 命令无法识别

**解决方案：** 安装 Node.js

- 访问：https://nodejs.org/
- 下载并安装 LTS 版本
- 重启终端

#### 问题：端口 3000 被占用

**解决方案：**

```bash
# 方法 1: 关闭占用 3000 端口的程序
# 方法 2: 使用其他端口
npm run dev -- --port 3001
```

#### 问题：地图显示空白或加载失败

**解决方案：**

1. 检查 `public/data/china_cities.json` 文件是否存在
2. 重新下载地图数据：
   ```bash
   node scripts/download-cities-detailed.js
   ```
3. 刷新浏览器（Ctrl + Shift + R 强制刷新）

---

## 📥 获取完整 GeoJSON 数据

✅ **好消息：地图数据已自动下载完成！**

当前项目已包含 **475 个地级市** 的完整数据，无需额外下载。

如果需要重新下载或更新数据：

### 推荐数据源

#### 1. 阿里云 DataV.GeoAtlas（推荐）

访问：http://datav.aliyun.com/portal/school/atlas/area_selector

**下载步骤：**

1. 打开上述网址
2. 选择「中华人民共和国」
3. 在右侧选择「全国」→「地级市」
4. 点击「复制 JSON」或「下载 JSON」
5. 将下载的文件重命名为 `china_cities.json`
6. 放置到项目的 `public/data/` 目录

#### 2. GitHub 开源资源

```bash
# 方案 A：使用 echarts-china-cities-js
git clone https://github.com/pyecharts/echarts-china-cities-js.git
# 从中提取 GeoJSON 数据

# 方案 B：使用 china-geojson
git clone https://github.com/longwosion/geojson-map-china.git
```

#### 3. 自定义精度数据

如果您只需要特定省份或城市：

- 访问 http://datav.aliyun.com/portal/school/atlas/area_selector
- 选择具体省份
- 下载该省的地级市数据
- 可以减小文件体积，提升加载速度

---

## 🚀 安装与运行

### 步骤 1：安装依赖

```bash
cd china-footprint-map
npm install
```

### 步骤 2：准备地图数据

确保 `public/data/china_cities.json` 文件存在且格式正确。

**验证 GeoJSON 格式：**

```bash
# 使用 Node.js 验证
node -e "console.log(JSON.parse(require('fs').readFileSync('./public/data/china_cities.json')).type)"
# 应输出: FeatureCollection
```

### 步骤 3：启动开发服务器

```bash
npm run dev
```

浏览器将自动打开 `http://localhost:3000`

### 步骤 4：构建生产版本

```bash
npm run build
npm run preview  # 预览构建结果
```

---

## 🎯 功能使用指南

### 基础操作

#### 1. 标记访问城市

- **点击地图上的城市**即可标记为「已访问」
- 已访问的城市会变为**蓝图蓝**渐变色
- 再次点击可以取消标记

#### 2. 查看城市信息

- **鼠标悬停**在城市上方
- 会显示自定义 Tooltip：
  - 城市名称
  - 访问状态
  - 访问日期（已访问城市）
  - 建筑笔记（已访问城市）

#### 3. 地图缩放与平移

- **鼠标滚轮**：缩放地图
- **拖拽**：平移地图
- **双击**：快速放大

#### 4. 导出数据

点击右上角「📤 导出数据」按钮：

- 数据会输出到浏览器控制台
- 同时自动下载 JSON 文件
- 文件名格式：`china_footprint_2026-02-24.json`

**导出数据格式：**

```json
{
  "visitedCities": [
    {
      "name": "南京市",
      "adcode": "320100",
      "visitDate": "2026-02-24T10:30:00.000Z",
      "notes": ""
    }
  ],
  "exportDate": "2026-02-24T10:30:00.000Z",
  "totalCities": 1
}
```

#### 5. 清空记录

点击「🗑️ 清空所有」按钮：

- 会弹出确认对话框
- 确认后清空所有访问记录
- ⚠️ 此操作不可恢复

---

## 🎨 自定义配色方案

如果您想修改配色，编辑以下文件：

### 修改主题色

`tailwind.config.js`:

```javascript
colors: {
  'paper': '#F4F1EA',           // 纸质背景色
  'blueprint-blue': '#2A5B8C',  // 蓝图蓝（已访问）
  'blueprint-light': '#4A7BA7', // 浅蓝（悬浮）
  'border-soft': '#D0D0D0',     // 边框色
}
```

### 修改地图样式

`src/components/ChinaMap.vue` 中的 `updateMapOption` 函数：

```javascript
itemStyle: {
  borderColor: '#D0D0D0',  // 边框颜色
  borderWidth: 0.5,         // 边框宽度
  areaColor: '#FFFFFF'      // 未访问城市颜色
}
```

---

## 🔧 常见问题解决

### 问题 1：地图显示「数据加载失败」

**原因：** GeoJSON 文件不存在或路径错误

**解决方案：**

1. 检查 `public/data/china_cities.json` 是否存在
2. 确认文件格式正确（使用 JSON 验证器）
3. 查看浏览器控制台的网络请求

### 问题 2：点击城市没有反应

**原因：** GeoJSON 中的 `properties.name` 字段缺失

**解决方案：**
确保 GeoJSON 每个 feature 都有：

```json
"properties": {
  "name": "城市名称",
  "adcode": "行政区划代码"
}
```

### 问题 3：地图边框太粗/太细

**解决方案：**
修改 `ChinaMap.vue` 中的 `borderWidth`:

```javascript
itemStyle: {
  borderWidth: 0.5; // 调整这个值 (0.3-2.0)
}
```

### 问题 4：数据未持久化

**原因：** 浏览器隐私模式或 LocalStorage 被禁用

**解决方案：**

1. 退出隐私/无痕模式
2. 检查浏览器设置中的 Cookie 和网站数据权限
3. 打开浏览器控制台查看错误信息

### 问题 5：地图加载缓慢

**原因：** GeoJSON 文件过大（完整中国地级市数据约 10-20MB）

**优化方案：**

1. **精简数据**：只保留需要的省份
2. **压缩 GeoJSON**：使用 Mapshaper (https://mapshaper.org/)
3. **使用 CDN**：将 GeoJSON 托管到 CDN 服务

---

## 📊 数据管理

### LocalStorage 位置

数据存储在浏览器的 LocalStorage：

- **Key**: `china_footprint_data`
- **查看方式**：浏览器 DevTools → Application → Local Storage

### 手动备份数据

```javascript
// 在浏览器控制台执行
const data = localStorage.getItem("china_footprint_data");
console.log(data); // 复制输出
```

### 手动恢复数据

```javascript
// 在浏览器控制台执行
const backupData = "..."; // 粘贴备份的 JSON
localStorage.setItem("china_footprint_data", backupData);
location.reload(); // 刷新页面
```

---

## 🚀 部署指南

### 部署到 Vercel

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### 部署到 Netlify

1. 构建项目：`npm run build`
2. 登录 Netlify
3. 将 `dist` 目录拖拽到 Netlify

### 部署到 GitHub Pages

```bash
# 1. 修改 vite.config.js，添加 base 路径
export default defineConfig({
  base: '/china-footprint-map/',
  // ...
})

# 2. 构建
npm run build

# 3. 部署到 gh-pages 分支
npx gh-pages -d dist
```

---

## 🎓 进阶功能开发建议

### 添加建筑笔记功能

在 `ChinaMap.vue` 的 Tooltip 中添加输入框：

```javascript
formatter: (params) => {
  return `
    <div class="custom-tooltip">
      <input 
        type="text" 
        placeholder="添加建筑笔记..."
        onchange="updateNotes('${cityName}', this.value)"
      />
    </div>
  `;
};
```

### 添加时间轴筛选

在 `useFootprint.js` 中添加：

```javascript
const filterByYear = (year) => {
  return visitedCities.value.filter(
    (city) => new Date(city.visitDate).getFullYear() === year,
  );
};
```

### 添加统计图表

集成 ECharts 饼图/柱状图展示：

- 各省份城市访问数量
- 按月份统计访问趋势
- 访问城市的地理分布热力图

---

## 📧 技术支持

如有问题，欢迎联系：

- 📧 Email: chenzehao7866@gmail.com
- 💼 LinkedIn: [Zehao Chen](https://www.linkedin.com/in/zehao-chen-9286603b0/)
- 🌐 Portfolio: [chenzehao7866.wixsite.com](https://chenzehao7866.wixsite.com/my-site)

---

**祝您使用愉快！ Happy Mapping! 🗺️✨**
